package com.brevy.batch.core.test;

import org.apache.ibatis.session.RowBounds;
import org.junit.Test;
import org.springframework.beans.factory.annotation.Autowired;

import com.brevy.batch.core.dao.QueryDao;
import com.brevy.batch.core.test.support.AbstractSpringContextTestSupport;

public class QueryDaoTestcase extends AbstractSpringContextTestSupport {

	@Test
	public void queryAll(){
		log.info(">>>> {}", new Object[]{queryDao.queryAll()});
	}
	
	@Test
	public void queryPaged(){
		RowBounds rowBounds = new RowBounds(2,2);
		log.info(">>>> {}", new Object[]{queryDao.queryPaged(rowBounds)});
	}
	
	@Autowired
	private QueryDao queryDao;
}
